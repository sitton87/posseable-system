/**
 * PosSEAble Design System
 * =======================
 * 
 * Export מרכזי של כל ה-Design System
 * 
 * @example
 * import { tw, cssVar, presets, card, numericValues, getIconSize } from '@/app/styles/design-system';
 */

export {
  // CSS Variable references
  cssVar,
  
  // Tailwind class mappings
  tw,
  
  // Numeric values for JS calculations
  numericValues,
  
  // Component style presets
  presets,
  
  // Card presets
  card,
  
  // Draft presets
  draft,
  
  // Task presets
  task,
  
  // Helper functions
  createTransition,
  getIconSize,
  getModalWidth,
  cn,
  
  // Types
  type TextColor,
  type BgColor,
  type BrandColor,
  type SemanticColor,
  type FontSize,
  type FontWeight,
  type Spacing,
  type Radius,
  type Shadow,
  type Duration,
  type IconSize,
  type ModalSize,
  type CssVar,
  type TwClasses,
  type NumericValues,
  type Presets,
  type CardPresetKey,
  type DraftPresetKey,
  type TaskPresetKey,
} from './theme';

